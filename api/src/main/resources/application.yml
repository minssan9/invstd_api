--- #local 환경
spring:
  profiles:
    group:
      "local" : "core, api, default-api, security, security-api, database, database-default, logging, mongo, api-group, aws, secret"

--- #dev 환경
spring:
  profiles:
    group:
      "develop" : "core, api, default-api, security, security-api, aws, secret"

--- #api 환경

spring:
  profiles:
    group:
      "release" : "core, api, default-api, security, security-api, aws"



